[pytest]
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli=true
log_cli_level=INFO
junit_logging=system-err
junit_family=legacy

addopts=
    -s
    -v
    --junitxml=py_test_results.xml
    --showlocals
    --durations=10
    --strict-markers
